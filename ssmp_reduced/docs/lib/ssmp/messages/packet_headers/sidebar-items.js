initSidebarItems({"struct":[["DataBlock","Data Blocks\"Data Blocks\" are used to represent both the payload and the user-defined space. The `DataBlock` struct defines a collection of arbitrary bytes. Both the user-defined header and the payload can optionally be filled with bytes when a packet is created."],["PacketHeader","Main SSMP Packet HeaderEvery SSMP packet will contain this header. It always sits directly at the top/front of the packet. It's laid out like so:"]]});